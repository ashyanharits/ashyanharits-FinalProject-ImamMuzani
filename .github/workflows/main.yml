name: 🚀 ImamMuzani

on:
  push:
    branches: [ main ]

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: 151.243.222.70
        username: ImamMuzani@syaandev.my.id
        password: ashyan310105
        exclude: |
          */.git
          */.git/**
          */node_modules/*
